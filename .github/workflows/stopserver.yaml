name: ServerStop

on:
    workflow_dispatch

jobs:
    start:
        runs-on: self-hosted
        timeout-minutes: 10

        steps:
          - name: Stop
            working-directory: ${{github.workspace}}/build
            run: sleep 30 && pkill server.out